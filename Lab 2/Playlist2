package Lab2

public class Playlist2 {

    private LinkedList<Songs>songs;

    public static void main(String[] args) {
        
        PlayList2 myPlayList2 = new MyPlayList2();

        Song s1 = new Song("Gravity", "Hazbin Hotel", "Jessica Vosk");
        Song s2 = new Song("Centuries", "American Beauty", "Patrick Stump");
        Song s3 = new Song("Your Idol", "Kpop Demon Hunters", "Andrew Choi");

        myPlayList.addToFront(S1);
        myPlayList.addToEnd(S2);
        myPlayList.addToEnd(S3);

        myPlaylist.playAll();

        System.out.println("\nPlaylist size: " + myPlayList.size());

        System.out.println("\nSkipping Song: ");
        myPlaylist.skip();

        myPlaylist.removeSong("Centuries");

        System.out.println("\nAfter removing Centuries: \n");

        myPlaylist.playAll();

        System.out.println("\nNow Playing:");
        System.out.println(myPlaylist.nowPlaying());        
    }

   public void addToFront(Song s){
        songs.addFirst(s);

        System.out.println("\nAdded to front:");
        playAll();
   }

   public void addToEnd(Song s){
        songs.addLast(s);

        System.out.println("\nAdded to end:");
        playAll();
   }

   public Song nowPlaying(){
        
    if(songs.isEmpty()){
        return null;
    }
    return songs.peekFirst();
   }

   public Song skip(){
        
     if(songs.isEmpty()){
        return null;
    }
    return songs.removeFirst();
   }


    public void playAll(){

        SongNode current = head;

       System.out.println("\nPlaylist:");

            for(Song song : songs){
                System.out.println(song);
                System.out.println("---------");
            }
        }
    

    public int size(){
        return songs.size();
    }

    public Song removeSong(String title){

        for(Song song : songs){

            if(song.getTitle().equalsIgnoreCase(title)){
                songs.remove(song);
                return song;
            }
        }

        return null;

        }
    }
